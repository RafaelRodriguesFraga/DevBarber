import styled from "styled-components/native";
import { Colors } from "../../shared/colors";

export const ScrollViewContainer = styled.ScrollView``;

export const SettingsButtonContainer = styled.TouchableOpacity``;

export const SettingsInfo = styled.View`
  padding: 20px 10px 10px 20px;  // top-right-bottom-left
`;

export const SettingsTitle = styled.Text`
  font-size: 17px;
  color: #F0FFF0;

`;

export const SettingsSubtitle = styled.Text`
  font-size: 14px;
  opacity: 0.5; 
`;
